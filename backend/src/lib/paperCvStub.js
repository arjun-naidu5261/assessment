/**
 * Placeholder for computer-vision pipeline (bubble detection / OMR).
 * Wire a real model or external service here; until then teachers validate manually.
 * @param {string} [_imagePath]
 */
export async function analyzeScanImage(_imagePath) {
  return {
    suggestions: [],
    note: "Automatic read not available — use teacher review to enter answers.",
  };
}
